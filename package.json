{
  "name": "goxai-lab",
  "version": "0.0.0",
  "private": true,
  "description": "An AI labeling studio",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm -r build",
    "check:r2-cors": "node scripts/check-r2-cors.mjs",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
