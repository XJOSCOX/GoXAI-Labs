# Cloudflare R2 setup

GoXAI uses Supabase for auth/Postgres and Cloudflare R2 for large dataset files.

## Environment

Fill these values in `.env`:

```env
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
R2_PUBLIC_BASE_URL=
```

`R2_PUBLIC_BASE_URL` is optional for now. Use it later if the bucket has a public/custom domain for read access.

## Bucket CORS

Direct browser uploads require CORS on the R2 bucket.

In Cloudflare:

1. Open **R2 Object Storage**.
2. Open your bucket.
3. Go to **Settings**.
4. Find **CORS policy**.
5. Add a policy like this for local development:

```json
[
  {
    "AllowedOrigins": ["http://localhost:5173"],
    "AllowedMethods": ["PUT", "GET", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3000
  }
]
```

Add the production web origin before deploying, for example:

```json
"AllowedOrigins": ["http://localhost:5173", "https://your-domain.com"]
```

## Current upload flow

1. The frontend requests `POST /api/assets/upload-url`.
2. The backend validates dataset access and signs a short-lived R2 PUT URL.
3. The browser uploads the file directly to R2.
4. The frontend registers the uploaded object through `POST /api/assets`.

Large files do not pass through the Express API.
